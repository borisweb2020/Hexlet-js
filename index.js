console.log('Hello, Hexlet!');

const fs = 'A new string ....';
console.log(fs);

const _ = require("lodash");
let p = _.intersection([2, 1], [2, 3]);
let g = _.last(['one', 'two']);
console.log(p, g);
